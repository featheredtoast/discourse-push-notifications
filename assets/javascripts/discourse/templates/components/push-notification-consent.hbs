{{#if showPushNotification}}
  {{#unless bannerDismissed}}
  {{#unless pushNotificationSubscribed}}
    {{i18n 'discourse_push_notifications.consent_prompt'}} <a {{action "subscribe"}}>{{i18n 'discourse_push_notifications.consent_enable'}}</a>.<a class="dismiss" {{action "dismiss"}}><i class="fa fa-times" aria-hidden="true"></i></a>
  {{/unless}}
  {{/unless}}
{{/if}}
